services:
  app:
    build: .
    container_name: wledger
    ports:
      - "3000:3000"
    volumes:
      # ONE volume handles DB (/app/data) AND Uploads (/app/data/uploads)
      - wledger-data:/app/data
    restart: unless-stopped

volumes:
  wledger-data: